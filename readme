【50】
发布新版本：
1、增加高级重组工具，辅助数值配表

【49】
发布新版本：
1、使用原有get接口读取数据报错的问题
2、加上导出时报错后，关闭文本流进程，避免出现进程被占用的问题

【48】
修改bug：
1、使用原有get接口读取数据报错的问题
2、加上导出时报错后，关闭文本流进程，避免出现进程被占用的问题

【47】
发布新版本：
1、将数据读取接口分开成两个，一个是读取xml的只读接口，一个是com的读写接口

【46】
发布新版本：
1、增加ListObject的数据范围是超上限的行或者列时的保护以及提示信息

【45】
发布新版本：
1、修复配置表转换逻辑中，每一次转换读取数据时都会重新读取表头，导致速度巨慢的问题

【44】
发布新版本：
1、修改底层数据读取方法，使用ExcelDataReader读取xml的方法
2、修改projectDefaultSetting的数据结构，json中没有的数据会动态创建，兼容老版本的数据

【43】
发布新版本
1、修复导入excel时少一行数据的bug
2、修复数据格式化枚举值时，超过255字符后，表重启会出错的bug
3、优化辅助输入时忽略大小写敏感

【42】
发布新版本
1、增加行字典忽略0值的参数

【41】
发布新版本：
1、增加判断字符串是否为空或者备注的方法
2、增加判断对象是否为空对象以及空字符串的方法

【40】
发布新版本：
1、AddRow方法在listobject中写入数据时使用文本格式
2、AddRow方法写入数据之后更新数据表
3、GetColumnData增加一个带版本参数的方法
4、生成新的DataTable，可以按行数量增加，也可以按照key数组增加

【39】
发布新版本：
1、增加行字典功能，能够将表头和数值连接起来

【38】
发布新版本，增加一键生成表头、数据导出配置、文件导出配置功能

【37】
发布新版本：
1、完成导出后自动弹出对比文件，以及调用打表工具的功能

【36】
发布新版本：
1、增加调起svn更新和提交的逻辑

【35】
发布新版本，修复点击导出按钮无反应的bug

【34】
发布新版本：
1、增加私有变量逻辑，按照计算机名为key，存储数据

【33】
发布新版本
1、增加ironpython调用的c#函数模块
2、增加了一个根据A列的值符合某个规则后筛选出行，取B列的数据，然后是否去重，返回一个列表的函数

【32】
发布新版本：
1、版本判断逻辑同时兼容版本号和版本组
2、增加python默认选中，以及方法说明、参数说明
3、增加数据添加和写入逻辑中，默认让该工作簿关闭时保存的逻辑

【31】
发布新版本
1：修复python写入数据的bug（错行）

【30】
发布新版本
1：修复python写入数据的bug（错行）

【29】
发布测试版本（生成数据）

【28】
发布新版本
修复版本组正则表达式bug

【27】
发布新版本：
修复版本组的bug

【25】
发布新版本，增加版本分组功能

【24】
发布新版本，修复各种异常情况报错（在没有工程的情况下的各种事件）

【23】
发布新版本，修复工程信息文件不存在的时候，点击任意单元格会报错的bug

【22】
修改bug，json文件导出时不转换数据的问题

【21】
json文件导入功能

【20】
发布新版本
1、修复bug，打开excel一定会保存的bug

【19】
发布新版本：
优化：
1、重构按钮状态显示逻辑
2、各种其他场景中也可以迅速调用按钮状态变化逻辑
3、保存文件时会自动关闭表头、数据等配置文件，并将工程数据保存
4、关闭文件前会自动关闭表头、数据等配置文件，并将工程数据保存
5、打开文件时，会自动删除之前遗留的配置数据
6、记录上次导出表的文件列表和版本列表
bug修复：
1、根据版本批量另存为csv功能，导出后会打开csv文件的bug
2、修改版本信息不生效
3、修复全选会报错的bug

【18】
发布新版本
1、修复导出时有时候数据未刷新的问题

【17】
发布新版本
1、增加数据查找方法
2、增加自动生成表头的功能
3、增加修改部分数据的功能和接口

【16】
发布新版本，增加随机函数

【15】
发布新版本：
增加Pyhton调用面板
增加奖励结构记录
增加奖励数据显示

【13】
发布新版本：
转换csv后关闭所有打开的工作簿，且增加转换完成的日志

【12】
发布新版本
1、捕捉log报错，让出错后不用重新打开也可以继续导出
2、去掉辅助输入按钮，功能入口放到右键菜单上
3、重构报错逻辑，去掉log文件，在面板上显示
4、重构按钮显隐文字的逻辑
5、打开按照版本批量导出csv的逻辑

【11】
发布新版本：
修改log，导出文件完成后会显示导出信息
修复表头转换关键字列无法辅助输入的bug
修改openedworkbook的实现方式，避免手动关闭工作簿后出现对象不为空，但数据异常的问题

【10】
发布新版本，修复表头设置修改后，导出时并未刷新的问题

【07】
发布新版本，id为0时不进行数据转换

【06】
发布新版本，点击单元格出现动态按钮的后，焦点会回到单元格上

【05】
发布新版本，处理多表头和多版本之间的各种问题
主要增加了data.GetTitleData方法

【04】
发布新版本，处理导出txt文件换行中的问题（excel中有换行会导致txt出错）

【02】
发布新版本，修复数据不刷新的问题